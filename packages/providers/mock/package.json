{
  "name": "@sentinel/provider-mock",
  "version": "0.1.0",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup index.ts --dts --format esm --sourcemap",
    "dev": "tsup index.ts --dts --format esm --sourcemap --watch",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "@sentinel/core": "workspace:*",
    "@sentinel/provider-types": "workspace:*"
  },
  "devDependencies": {
    "rimraf": "^6.0.1",
    "tsup": "^8.5.0",
    "typescript": "^5.6.3"
  },
  "license": "MIT"
}
